// Phase 5+ placeholder — OpenCV scene segmentation + NCNN multimodal guard.
// This file exists so the CMake project is buildable now; real implementation
// arrives when audio-LLM uncertainty triggers visual confirmation.
namespace prism {
int stub_marker() { return 0; }
}
